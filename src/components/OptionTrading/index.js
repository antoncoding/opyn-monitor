/* eslint-disable no-underscore-dangle */
import React, { useMemo, useState } from 'react';

// import Web3Providers from 'web3-providers';
// const Web3Providers = require('web3-providers');
// import { WSClient } from '@0x/mesh-rpc-client';

// const client = new WSClient('ws://localhost:60559');

// const websocketProvider = new Web3Providers.WebsocketProvider('ws://localhost:60559');

function OptionTrading({ user, oTokens }) {
  // const [ws, setWS] = useState({});

  useMemo(async () => {

  }, []);

  return (
    <div>
      <h1>
        Testing
      </h1>
    </div>
  );
}

export default OptionTrading;
